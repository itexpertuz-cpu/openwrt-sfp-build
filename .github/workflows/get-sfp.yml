- name: Inspect tree
  run: |
    cd openwrt

    echo "==== include ===="
    ls include

    echo
    echo "==== kernel files ===="
    find include -iname "*kernel*"

    echo
    echo "==== grep LINUX_VERSION ===="
    grep -R "LINUX_VERSION" include target -n | head -50
